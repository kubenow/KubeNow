- name: "get test timestamp"
  set_fact:
    test_timestamp: "{{lookup('pipe','date +%Y%m%d%H%M%S')}}"
- name: "print host_cloud"
  debug: var=host_cloud
- name: "print timestamp"
  debug: var=test_timestamp
