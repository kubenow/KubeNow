---
# This tests also weave-network and traefik-lb (implicitly)
# TODO: make explicit tests for weave-network and traefik-lb
- hosts: localhost
  connection: local
  gather_facts: False
  force_handlers: True
  vars:
    node_count: 6
    cf_zone: uservices.cloud
  pre_tasks:
    - include: "{{playbook_dir}}/pre_tasks/get_timestamp.yml"
  roles:
    - test-packer
    - test-terraform
    - test-bootstrap
    - test-cheese
