- hosts: localhost
  connection: local
  gather_facts: False
  force_handlers: True
  vars:
    host_cloud: openstack
    node_count: 6 # see roles/test-terraform/templates
  roles:
    - test-terraform
    - test-bootstrap
